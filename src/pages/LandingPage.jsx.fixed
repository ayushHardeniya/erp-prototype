import React from 'react';
import { Link } from 'react-router-dom';
import { motion } from 'framer-motion';
import { 
  AcademicCapIcon, 
  ClockIcon, 
  CurrencyRupeeIcon, 
  DocumentChartBarIcon, 
  BuildingLibraryIcon, 
  UserGroupIcon,
  CalendarIcon,
  BellAlertIcon,
  TruckIcon,
  QuestionMarkCircleIcon
} from '@heroicons/react/24/outline';

// Import Solvix logo if available
// import SolvixLogo from '../assets/images/Solvix SIH.png';

const features = [
  {
    title: "Student Management",
    description: "Complete student lifecycle management with detailed profiles, attendance tracking, and performance analytics.",
    icon: <UserGroupIcon className="h-8 w-8 text-primary-500" />
  },
  {
    title: "Attendance System",
    description: "Modern attendance tracking with QR code-based check-ins and real-time reporting.",
    icon: <ClockIcon className="h-8 w-8 text-primary-500" />
  },
  {
    title: "Timetable Planning",
    description: "Intelligent class scheduling, teacher assignments, and room allocation with conflict prevention.",
    icon: <CalendarIcon className="h-8 w-8 text-primary-500" />
  },
  {
    title: "Exams & Results",
    description: "End-to-end examination management from scheduling to result publishing with detailed analytics.",
    icon: <DocumentChartBarIcon className="h-8 w-8 text-primary-500" />
  },
  {
    title: "Fees & Payments",
    description: "Streamlined fee collection with various payment options and automated reminders.",
    icon: <CurrencyRupeeIcon className="h-8 w-8 text-primary-500" />
  },
  {
    title: "Library Management",
    description: "Complete library system with catalog management, issue tracking, and digital resources.",
    icon: <BuildingLibraryIcon className="h-8 w-8 text-primary-500" />
  },
  {
    title: "Hostel Management",
    description: "Comprehensive hostel management including room allocation, attendance, and fee collection.",
    icon: <AcademicCapIcon className="h-8 w-8 text-primary-500" />
  },
  {
    title: "Notifications",
    description: "Multi-channel notification system for important announcements and updates.",
    icon: <BellAlertIcon className="h-8 w-8 text-primary-500" />
  },
  {
    title: "Transport Management",
    description: "Vehicle tracking, route optimization, and transport fee management.",
    icon: <TruckIcon className="h-8 w-8 text-primary-500" />
  },
  {
    title: "Support System",
    description: "Integrated helpdesk for students, parents, and staff with ticket tracking.",
    icon: <QuestionMarkCircleIcon className="h-8 w-8 text-primary-500" />
  }
];

const LandingPage = () => {
  return (
    <div className="min-h-screen bg-gray-50">
      {/* Header/Navigation */}
      <header className="bg-white shadow-sm sticky top-0 z-10">
        <nav className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between">
          <div className="flex items-center">
            <span className="text-2xl font-bold text-primary-600">Solvix ERP</span>
            {/* <img src={SolvixLogo} alt="Solvix Logo" className="h-10" /> */}
          </div>
          <div className="hidden md:flex items-center space-x-8">
            <a href="#features" className="text-gray-600 hover:text-primary-600">Features</a>
            <a href="#testimonials" className="text-gray-600 hover:text-primary-600">Testimonials</a>
            <a href="#pricing" className="text-gray-600 hover:text-primary-600">Pricing</a>
            <a href="#contact" className="text-gray-600 hover:text-primary-600">Contact</a>
          </div>
          <div className="flex items-center space-x-4">
            <Link to="/login"
              className="px-4 py-2 rounded border border-primary-500 text-primary-600 hover:bg-primary-50 transition duration-200"
            >
              Sign In
            </Link>
            <Link to="/register"
              className="px-4 py-2 rounded bg-primary-600 text-white hover:bg-primary-700 transition duration-200"
            >
              Register
            </Link>
          </div>
        </nav>
      </header>

      {/* Hero Section */}
      <section className="relative overflow-hidden bg-gradient-to-r from-primary-600 to-primary-800 text-white">
        <div className="absolute inset-0">
          <div className="absolute inset-0 bg-gradient-to-r from-primary-600 to-primary-800 opacity-90"></div>
        </div>
        <div className="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-24 md:py-32">
          <div className="md:w-3/5">
            <motion.h1 
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5 }}
              className="text-4xl md:text-6xl font-extrabold mb-6"
            >
              Transforming Education Management
            </motion.h1>
            <motion.p 
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5, delay: 0.2 }}
              className="text-xl md:text-2xl mb-8 text-primary-100"
            >
              A comprehensive ERP solution designed for modern educational institutions that simplifies administration and enhances student experience
            </motion.p>
            <motion.div 
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5, delay: 0.4 }}
              className="flex flex-col sm:flex-row gap-4"
            >
              <Link to="/register"
                className="px-6 py-3 text-center rounded-md bg-white text-primary-600 hover:bg-gray-100 font-medium transition duration-200"
              >
                Get Started Free
              </Link>
              <a 
                href="#features" 
                className="px-6 py-3 text-center rounded-md border border-white text-white hover:bg-white/10 font-medium transition duration-200"
              >
                Learn More
              </a>
            </motion.div>
          </div>
        </div>
      </section>

      {/* Features Section */}
      <section id="features" className="py-20">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-16">
            <h2 className="text-3xl md:text-4xl font-bold mb-4 text-gray-900">Comprehensive Features</h2>
            <p className="text-xl text-gray-600 max-w-3xl mx-auto">
              Solvix ERP provides a complete suite of tools to manage every aspect of educational institutions
            </p>
          </div>
          
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {features.map((feature, index) => (
              <motion.div 
                key={index}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                viewport={{ once: true }}
                className="bg-white p-6 rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300"
              >
                <div className="mb-4">{feature.icon}</div>
                <h3 className="text-xl font-semibold mb-2 text-gray-900">{feature.title}</h3>
                <p className="text-gray-600">{feature.description}</p>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Testimonials */}
      <section id="testimonials" className="py-20 bg-gray-100">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-16">
            <h2 className="text-3xl md:text-4xl font-bold mb-4 text-gray-900">Trusted by Educators</h2>
            <p className="text-xl text-gray-600 max-w-3xl mx-auto">
              See what educational institutions say about Solvix ERP
            </p>
          </div>
          
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            <motion.div 
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              transition={{ duration: 0.5 }}
              viewport={{ once: true }}
              className="bg-white p-6 rounded-lg shadow-md"
            >
              <div className="flex items-center mb-4">
                <div className="h-12 w-12 rounded-full bg-primary-100 flex items-center justify-center text-primary-700 font-bold text-xl">
                  AB
                </div>
                <div className="ml-4">
                  <h4 className="font-semibold">Dr. Amit Bhatnagar</h4>
                  <p className="text-sm text-gray-600">Principal, Delhi Public School</p>
                </div>
              </div>
              <p className="text-gray-600">
                "Solvix ERP has transformed how we manage our institution. The integrated modules have significantly reduced administrative workload and improved student engagement."
              </p>
            </motion.div>

            <motion.div 
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              transition={{ duration: 0.5, delay: 0.2 }}
              viewport={{ once: true }}
              className="bg-white p-6 rounded-lg shadow-md"
            >
              <div className="flex items-center mb-4">
                <div className="h-12 w-12 rounded-full bg-primary-100 flex items-center justify-center text-primary-700 font-bold text-xl">
                  PK
                </div>
                <div className="ml-4">
                  <h4 className="font-semibold">Prof. Priya Kumar</h4>
                  <p className="text-sm text-gray-600">Dean, Sri Venkateswara College</p>
                </div>
              </div>
              <p className="text-gray-600">
                "The analytics and reporting capabilities have given us insights that help make data-driven decisions. Support team is responsive and implementation was smooth."
              </p>
            </motion.div>

            <motion.div 
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              transition={{ duration: 0.5, delay: 0.4 }}
              viewport={{ once: true }}
              className="bg-white p-6 rounded-lg shadow-md"
            >
              <div className="flex items-center mb-4">
                <div className="h-12 w-12 rounded-full bg-primary-100 flex items-center justify-center text-primary-700 font-bold text-xl">
                  RJ
                </div>
                <div className="ml-4">
                  <h4 className="font-semibold">Rajesh Joshi</h4>
                  <p className="text-sm text-gray-600">IT Director, Symbiosis Institute</p>
                </div>
              </div>
              <p className="text-gray-600">
                "From a technical perspective, Solvix ERP is well-designed with excellent security features and integration capabilities. It's been reliable even with our large user base."
              </p>
            </motion.div>
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section id="cta" className="py-20 bg-primary-600 text-white">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <motion.h2 
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            transition={{ duration: 0.5 }}
            viewport={{ once: true }}
            className="text-3xl md:text-4xl font-bold mb-6"
          >
            Ready to transform your institution?
          </motion.h2>
          <motion.p 
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            transition={{ duration: 0.5, delay: 0.2 }}
            viewport={{ once: true }}
            className="text-xl mb-8 max-w-3xl mx-auto"
          >
            Join hundreds of educational institutions that trust Solvix ERP to streamline their operations
          </motion.p>
          <motion.div 
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            transition={{ duration: 0.5, delay: 0.4 }}
            viewport={{ once: true }}
          >
            <Link to="/register"
              className="px-6 py-3 bg-white text-primary-600 rounded-md font-medium hover:bg-gray-100 transition duration-200"
            >
              Get Started Today
            </Link>
          </motion.div>
        </div>
      </section>

      {/* Footer */}
      <footer id="contact" className="bg-gray-900 text-white py-12">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="grid grid-cols-1 md:grid-cols-4 gap-8">
            <div>
              <h4 className="text-lg font-semibold mb-4">Solvix ERP</h4>
              <p className="text-gray-400">
                Building the future of education management with innovative technology solutions.
              </p>
            </div>
            <div>
              <h4 className="text-lg font-semibold mb-4">Quick Links</h4>
              <ul className="space-y-2">
                <li><a href="#" className="text-gray-400 hover:text-white transition duration-200">About Us</a></li>
                <li><a href="#features" className="text-gray-400 hover:text-white transition duration-200">Features</a></li>
                <li><a href="#testimonials" className="text-gray-400 hover:text-white transition duration-200">Testimonials</a></li>
                <li><a href="#" className="text-gray-400 hover:text-white transition duration-200">Blog</a></li>
              </ul>
            </div>
            <div>
              <h4 className="text-lg font-semibold mb-4">Support</h4>
              <ul className="space-y-2">
                <li><a href="#" className="text-gray-400 hover:text-white transition duration-200">Help Center</a></li>
                <li><a href="#" className="text-gray-400 hover:text-white transition duration-200">Documentation</a></li>
                <li><a href="#" className="text-gray-400 hover:text-white transition duration-200">API Reference</a></li>
                <li><a href="#" className="text-gray-400 hover:text-white transition duration-200">Contact Support</a></li>
              </ul>
            </div>
            <div>
              <h4 className="text-lg font-semibold mb-4">Contact</h4>
              <address className="not-italic text-gray-400">
                <p>123 Education Street</p>
                <p>New Delhi, India</p>
                <p className="mt-2">Email: info@solvixerp.com</p>
                <p>Phone: +91 98765 43210</p>
              </address>
            </div>
          </div>
          <div className="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400">
            <p>&copy; {new Date().getFullYear()} Solvix ERP. All rights reserved.</p>
          </div>
        </div>
      </footer>
    </div>
  );
};

export default LandingPage;